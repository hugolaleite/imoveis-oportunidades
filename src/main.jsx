import React from "react";
import ReactDOM from "react-dom/client";
import ImoveisOportunidade from "./ImoveisOportunidade";
import "./style.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ImoveisOportunidade />
  </React.StrictMode>
);
